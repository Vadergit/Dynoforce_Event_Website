{
  "name": "dynogrip-event-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prepare:event-ui": "node scripts/prepare-event-ui.mjs",
    "dev": "npm run prepare:event-ui && vite",
    "build": "npm run prepare:event-ui && vite build",
    "preview": "vite preview",
    "provision:venues": "node scripts/provision-venue-accounts.mjs"
  },
  "dependencies": {
    "firebase": "^12.0.0",
    "jspdf": "^4.2.1",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
